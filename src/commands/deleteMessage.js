// Delete Message Command
const { Client, Message } = require('discord.js');

module.exports = {
  name: 'delete',
  description: 'Delete a message in the server',
  execute(message, args) {
    // Logic to delete message
  }
}